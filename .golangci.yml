# golangci-lint configuration for Freightliner project
# Compatible with golangci-lint v2.3.0

version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - errcheck
    - gosec
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused
  disable:
    # Temporarily disable linters that might cause export data issues
    - bodyclose
    - dupl
    - gocyclo
    - nakedret
    - prealloc
    - revive
    - unconvert
    - unparam